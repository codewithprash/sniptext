{
  "name": "sniptext",
  "version": "1.0.0",
  "description": "SnipText OCR API - AI-powered screenshot to text",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "db:migrate": "wrangler d1 execute sniptext --remote --file=migrations/0001_initial_schema.sql",
    "db:local": "wrangler d1 execute sniptext --local --file=migrations/0001_initial_schema.sql",
    "db:reset": "wrangler d1 execute sniptext --remote --command \"DROP TABLE IF EXISTS error_logs; DROP TABLE IF EXISTS subscriptions; DROP TABLE IF EXISTS usage_daily; DROP TABLE IF EXISTS auth_sessions; DROP TABLE IF EXISTS users;\"",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.11.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.54.0"
  }
}
